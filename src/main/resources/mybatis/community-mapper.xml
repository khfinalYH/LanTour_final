<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="community">


<!-- 
community_no, member_no, ,community_title, community_content, community_regdate, communtiy_readcount, community_deflag -->
<select id = "community_selectList" resultType="CommunityDto">
	SELECT COMMUNITY_NO, MEMBER_NO, COMMUNITY_TITLE, COMMUNITY_CONTENT, COMMUNITY_REGDATE, COMMUNITY_READCOUNT, COMMUNITY_DEFLAG
	FROM COMMUNITY
	WHERE COMMUNITY_NO
	ORDER BY COMMUNITY_NO DESC
</select>

<select id ="community_selectOne" parameterType="int" resultType="CommunityDto">
	SELECT COMMUNITY_NO, MEMBER_NO, COMMUNITY_TITLE, COMMUNITY_CONTENT, COMMUNITY_REGDATE, COMMUNITY_READCOUNT, COMMUNITY_DEFLAG
	FROM COMMUNITY
	WHERE COMMUNITY_NO = #{notice_no}
</select>

<insert id="community_insert" parameterType="CommunityDto">
	INSERT INTO COMMUNITY
	VALUES(COMMUNITY_NO.NEXTVAL, #{memeber_no}, #{community_title}, #{community_content}, sysdate)
</insert>

<delete id="community_delete" parameterType="int">
	DELETE FROM COMMUNITY
	WHERE COMMUNITY_NO = #{community_no}
</delete>

<update id="community_update" parameterType="int">
	UPDATE COMMUNITY
	SET COMMUNITY_TITLE = #{community_title}, COMMUNITY_CONTENT = #{community_title} 
</update>

</mapper>
