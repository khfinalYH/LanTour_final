<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="member">

	<select id="selectList" resultType="memberDto">
		SELECT MEMBER_NO, MEMBER_ID, MEMBER_NAME, MEMBER_PASSWORD, MEMBER_GRADE,
		MEMBER_AGE, MEMBER_GENDER, MEMBER_EMAIL, MEMBER_PHONE,
		MEMBER_SECESSION
		FROM MEMBER
		ORDER BY MYNO DESC
	</select>

	<select id="selectOne" parameterType="int"
		resultType="memberDto">
		SELECT MEMBER_NO, MEMBER_ID, MEMBER_NAME, MEMBER_PASSWORD, MEMBER_GRADE,
		MEMBER_AGE, MEMBER_GENDER, MEMBER_EMAIL, MEMBER_PHONE,
		MEMBER_SECESSION
		FROM MEMBER
		WHERE MEMBER_NO = #{member_no}
	</select>

	<select id="idCheck" parameterType="String"
		resultType="memberDto">
		SELECT MEMBER_NO, MEMBER_ID, MEMBER_NAME, MEMBER_PASSWORD, MEMBER_GRADE,
		MEMBER_AGE, MEMBER_GENDER, MEMBER_EMAIL, MEMBER_PHONE,
		MEMBER_SECESSION
		FROM MEMBER
		WHERE MEMBER_ID = #{member_id}
	</select>

	<insert id="insert" parameterType="memberDto">
		INSERT INTO MEMEBER
		VALUES(MEMBER_NO_SEQ.NEXTVAL,#{member_name},#{member_id},#{member_password},#{member_grade},#{member_gender},#{member_email},#{member_phone},#{member_secssion})
	</insert>

	<update id="update" parameterType="memberDto">
		UPDATE MEMBER
		SET MEMBER_PASSWORD = #{member_password} WHERE MEMBER_NO = #{memebr_no}
	</update>

	<update id="delete" parameterType="int">
		UPDATE MMBER
		SET MEMBER_SECESSION = 'N' WHERE MEMBER_NO = #{memebr_no}
	</update>

</mapper>
